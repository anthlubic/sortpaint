{
  "name": "sortpaint-leaderboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "schema": "wrangler d1 execute sortpaint-leaderboard --file=schema.sql --remote"
  },
  "devDependencies": {
    "vitest": "^2.1.9",
    "wrangler": "^4.44.0"
  }
}
